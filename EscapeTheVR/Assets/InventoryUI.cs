using UnityEngine;

public class InventoryUI : MonoBehaviour
{

    Inventar inventar;
    // Start is called before the first frame update
    void Start()
    {
        //inventar = Inventar.instance;   
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
